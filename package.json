{
  "name": "pe-learning-path",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=22.22.0 <23.0.0"
  },
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev": "npm run dev --workspaces --if-present",
    "cdk": "cdk",
    "synth": "cdk synth",
    "deploy": "cdk deploy"
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "aws-cdk": "^2.1106.1",
    "aws-cdk-lib": "^2.239.0",
    "constructs": "^10.5.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
